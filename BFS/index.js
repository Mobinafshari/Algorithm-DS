const graph = {
  A: ["B", "C"],
  B: ["D", "E"],
  C: ["F"],
  D: [],
  E: ["F"],
  F: []
};


function bfs(graph, start) {
  const visited = new Set();
  const queue = [];

  

  return Array.from(visited); 
}
